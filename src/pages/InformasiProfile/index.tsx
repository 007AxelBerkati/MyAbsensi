import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const InformasiProfile = (props: Props) => {
  return (
    <View>
      <Text>InformasiProfile</Text>
    </View>
  );
};

export default InformasiProfile;

const styles = StyleSheet.create({});
