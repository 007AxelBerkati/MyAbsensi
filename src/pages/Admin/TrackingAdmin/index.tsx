import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TrackingAdmin = ({navigation}: any) => {
  return (
    <View>
      <Text>TrackingAdmin</Text>
    </View>
  );
};

export default TrackingAdmin;

const styles = StyleSheet.create({});
