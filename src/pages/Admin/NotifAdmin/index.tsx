import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const NotifAdmin = (props: Props) => {
  return (
    <View>
      <Text>NotifAdmin</Text>
    </View>
  );
};

export default NotifAdmin;

const styles = StyleSheet.create({});
